import 'package:flutter/material.dart';

class FavorutesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('The Favorites'),
    );
  }
}
